// Import the Google and Meta classes from the dist/src module
import { Google, Meta } from "./dist/src";

// Export the Google and Meta classes to make them available to other modules
export {
    Google, // Export the Google class for external use
    Meta    // Export the Meta class for external use
}
